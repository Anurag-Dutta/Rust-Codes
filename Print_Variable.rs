fn main()
{
    let a = 45; // Here the Variable is Immutable
    println!("The Value stored in the Variable is {}", a);
    // a= a + 8;
    // println!("The Value stored in the Variable is {}", a); // This Line will give error as Immutable Variables can't be modified
}